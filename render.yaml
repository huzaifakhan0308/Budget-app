databases:
  - name: postgres
    databaseName: budget_app_development
    user: recipe_app

services:
  - type: web
    name: Budget app
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: 38b5691ed7a544bc5a7fb6c863abee5d
        sync: false
